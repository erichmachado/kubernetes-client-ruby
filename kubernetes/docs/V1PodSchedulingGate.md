# Kubernetes::V1PodSchedulingGate

## Properties

| Name | Type | Description | Notes |
| ---- | ---- | ----------- | ----- |
| **name** | **String** | Name of the scheduling gate. Each scheduling gate must have a unique name field. |  |

## Example

```ruby
require 'kubernetes'

instance = Kubernetes::V1PodSchedulingGate.new(
  name: null
)
```

